# 📇 Digital Business Card

**A sleek, responsive digital business card to showcase your professional profile.**

**Status:** Complete

---

## 🎯 The Problem & Solution

**The Problem:** Physical business cards are easily lost and hard to update.

**The Solution:** A digital profile card that is always accessible, easy to share, and interactive.

---

## ✨ Key Features

*   **Clean Layout:** Uses Flexbox to align the profile picture and text perfectly.
*   **Hover Effects:** The card slightly lifts and scales up when hovered, adding a tactile feel.
*   **Responsive Image:** Profile picture is automatically sized to fit the design.

---

## 🧠 Learnings

*   **Flexbox Alignment:** Mastered `justify-content: space-around` and `align-items: center` to create a balanced layout.
*   **CSS Transitions:** Used `transition: all 0.3s ease-in-out` to make the hover effects smooth instead of jerky.
*   **Transformations:** Implemented `transform: scale()` and `translateY()` to create the "lifting" animation effect.
*   **Box Shadow:** Added `box-shadow` to give depth to the card, especially on hover.

---

## 💻 Local Setup Instructions

1.  **Open** the `index.html` file in your browser.
2.  **Hover** over the card to see the animation!
